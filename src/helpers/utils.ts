const add = (val1: number, val2: number): any => val1 + val2;

const subtract = (val1: number, val2: number) => val1 + val2;
const multiply = (val1: number, val2: number) => val1 + val2;

export { add, subtract, multiply };
